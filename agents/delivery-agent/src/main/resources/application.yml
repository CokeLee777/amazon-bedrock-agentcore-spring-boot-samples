server:
  port: ${AGENT_PORT:9002}

spring:
  ai:
    bedrock:
      aws:
        region: ${BEDROCK_REGION:ap-northeast-2}
      converse:
        chat:
          options:
            model: ${BEDROCK_MODEL_ID:amazon.nova-pro-v1:0}

a2a:
  agent-url: ${AGENT_URL:http://localhost:9002}
